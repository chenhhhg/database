spring:
  profiles:
    active: @profiles.active@

server:
  port: 8001

# JWT配置
jwt:
  secret: your-256-bit-secret-key-here # 建议使用复杂随机字符串
  expiration: 86400000 # 24小时（单位：毫秒）

mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto
